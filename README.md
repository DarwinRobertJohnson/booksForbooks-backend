# booksForbooks-backend
A back end application for a social media web app for bookworms, built with django framework. 

# Setup
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver

# NOTE : Host the backend at http://127.0.0.1:8000/, the frontend application targets that for fetching.
